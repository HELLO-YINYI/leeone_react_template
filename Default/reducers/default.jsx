import * as actionTypes from '../contains/default'

let initialState = {};
export default function userinfo(state=initialState, action) {
    switch(action.type) {

        //默认
        default:
            return state;
    }
}